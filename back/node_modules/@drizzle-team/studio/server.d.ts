import { Context, Env, Hono } from "hono";
import type { QueryEngine } from "./common";
export type Server = {
    start: (params: {
        host: string;
        port: number;
        key?: string;
        cert?: string;
        cb: (err: Error | null, address: string) => void;
    }) => void;
};
export declare const prepareServer: (queryEngineProvider: (c: Context<Env, "/", {}>) => Promise<QueryEngine>, app?: Hono) => Promise<Server>;
